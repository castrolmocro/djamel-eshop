[phases.build]
cmds = [
  "pnpm add -g node-gyp",
  "pnpm install --frozen-lockfile",
  "pnpm --filter @workspace/mockup-sandbox build"
]

[deploy]
startCmd = "pnpm --filter @workspace/mockup-sandbox dev"
